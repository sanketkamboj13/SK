"use client"; import { useState } from "react"; import { Header, Footer, ProductCard } from "../components"; import { products } from "../../lib/products";
export default function Search(){const [q,setQ]=useState("");const list=products.filter(p=>p.name.toLowerCase().includes(q.toLowerCase())||p.category.toLowerCase().includes(q.toLowerCase()));return <><Header/><main className="listing search-page"><p className="eyebrow">S.K / SEARCH</p><h1>FIND YOUR NEXT FIT</h1><input autoFocus className="search-input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search shirts, jeans, t-shirts..."/><p>{q?`${list.length} results`:"Try “black shirt” or “oversized tee”"}</p><div className="grid four">{q&&list.map(p=><ProductCard key={p.id} p={p}/>)}</div></main><Footer/></>}

"use client"; import { Header, Footer, ProductCard } from "../components"; import { products } from "../../lib/products"; import { useStore } from "../store"; import Link from "next/link";
export default function Wishlist(){const {wishlist}=useStore();const list=products.filter(p=>wishlist.includes(p.id));return <><Header/><main className="listing"><div className="listing-head"><div><p className="eyebrow">S.K / SAVED</p><h1>WISHLIST</h1></div></div>{list.length?<div className="grid four">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="empty"><p>Your wishlist is empty.</p><Link className="btn" href="/shop">EXPLORE SHOP</Link></div>}</main><Footer/></>}

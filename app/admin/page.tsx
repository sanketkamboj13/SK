"use client";
import { products } from "../../lib/products"; import { Header, Footer } from "../components";
export default function Admin(){const revenue=products.slice(0,18).reduce((a,p)=>a+(p.salePrice||p.price),0);return <><Header/><main className="admin"><div className="admin-head"><div><p className="eyebrow">S.K / ADMIN</p><h1>COMMAND CENTER</h1></div><button className="btn">+ ADD PRODUCT</button></div><div className="kpis">{[["TOTAL SALES",`₹${revenue.toLocaleString("en-IN")}`],["ORDERS","128"],["CUSTOMERS","642"],["PRODUCTS","50"],["LOW STOCK","7"],["CONVERSION","3.8%"]].map(x=><div className="kpi" key={x[0]}><p>{x[0]}</p><strong>{x[1]}</strong><small>↑ 12.4% vs last period</small></div>)}</div><div className="admin-grid"><section className="panel"><h2>PRODUCT MANAGEMENT</h2><div className="admin-table">{products.slice(0,10).map(p=><div className="row" key={p.id}><span>{p.sku}</span><b>{p.name}</b><span>{p.category}</span><span>₹{p.price}</span><span>{p.stock}</span><button>EDIT</button></div>)}</div></section><section className="panel"><h2>SALES OVER TIME</h2><div className="bars">{[42,65,48,82,70,94,76,100,88,72,96,110].map((n,i)=><i key={i} style={{height:`${n}%`}}/>)}</div></section></div></main><Footer/></>}
